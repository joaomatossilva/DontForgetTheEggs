@model IEnumerable<DontForgetTheEggs.Core.ViewModels.GroceryListOverview>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Completed)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IngredientsCount)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Completed)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IngredientsCount)
        </td>
        <td>
            @Html.ActionLink("Details", "Index", "GroceryList", new { id = item.Id }, null) |
            @Html.ActionLink("Delete", "Delete", "GroceryList", new { id = item.Id }, null)
        </td>
    </tr>
}

</table>
